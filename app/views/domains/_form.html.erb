<%= form_for(domain) do |f| %>
  <% if domain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(domain.errors.count, "error") %> prohibited this domain from being saved:</h2>

      <ul>
      <% domain.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :descript %>
    <%= f.text_area :descript %>
  </div>

  <div class="field">
    <%= f.label :domain_type_id %>
    <%= f.text_field :domain_type_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
